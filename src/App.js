import logo from './logo.svg';
import './App.css';

function App() {

    //functions that call to the backend go here

    //buttons could initiate a route path change on click

    return (
        <div className="App">
            <header className="App-header">
                <img src={logo} className="App-logo" alt="logo" />
                <p>
                    This is a base for the caregiver app.
                </p>
            </header>
        </div>
    );
}

export default App;
